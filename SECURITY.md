# 安全政策

## 支持版本

安全修复面向最新发布版本和 `main` 分支。旧版本不保证单独回溯修复。

## 报告安全漏洞

请不要在公开 Issue、Discussion、日志或截图中披露漏洞、API Key、书籍内容或其他私人数据。

请在 GitHub 仓库的 **Security** 页面选择 **Report a vulnerability**，通过 Private Vulnerability Reporting 提交。报告中请包含：

- 受影响的版本或提交；
- 最小复现步骤与预期影响；
- 已移除私人数据的日志或合成测试文件；
- 可行的缓解或修复建议（如有）。

维护者会尽快确认报告，在完成影响评估和修复前与报告者保持私密沟通，并在修复发布后公开致谢（报告者要求匿名时除外）。

## 安全边界

FolioPaw 是无账号的单人本地应用，默认仅监听本机地址。它不应直接暴露到互联网。跨设备使用必须由可信反向代理、HTTPS、身份验证和防火墙共同保护。

以下问题属于安全报告范围：

- 未授权的网络访问或默认端口意外暴露；
- EPUB/PDF 上传导致的路径穿越、压缩炸弹或任意文件访问；
- API Key、书籍正文、数据库或内部下载地址泄露；
- 模型引导服务的校验绕过、命令注入或不可信模型导入；
- 可复现的依赖或容器供应链风险。

仅涉及本机已有管理员权限、用户主动配置的不可信模型 API，或公开文档已经说明的无账号部署边界，通常不视为漏洞。
