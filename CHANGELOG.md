# 更新日志

本项目使用 Keep a Changelog 的结构，并计划采用语义化版本。

## [尚未发布]

### 新增

- AI 摘要支持语音朗读。AI摘要弹窗、智能摘要面板的全书摘要卡片、章节摘要条目和阅读弹窗新增朗读按钮，可播放、暂停、继续和停止；语音通过 Microsoft Edge 在线语音服务免费合成（默认中文女声晓晓），朗读前自动去除 Markdown 排版符号。关闭弹窗或面板后合成与播放在后台继续，由右下角全局迷你播放条控制，直到播完或手动停止。
- README 新增自动更新的 Star 增长趋势图，使用仓库自身的 GitHub Actions 记录变化，不需要在公开文档中嵌入访问令牌。

### 修复

- 思考型模型（如 DeepSeek）批量翻译不再反复失败。此前 OpenAI 兼容通道不传结构化输出约束，模型返回的 JSON 会因裸换行或截断而解析失败；思考内容耗尽输出预算时还会返回空文本，只报模糊的「没有返回文本内容」。现在请求带 `response_format: json_object`，JSON 解析对字符串内裸换行做修复容错，单页翻译预算从 4000 提高到 8000 词元；检测到思考耗尽预算时自动关闭思考重试一次，报错信息也能区分「输出被截断」与「只有思考没有答案」。
- 朗读含 `&`、`<`、`>` 等字符的摘要不再失败。此前这些字符会生成非法 SSML，语音服务直接断开连接并返回空音频，前端报「Failed to load because no supported source was found」；现在合成前会转义特殊字符，空音频也会以中文报错提示而不是返回无法播放的空响应。
- 内存缓存写满后不再让翻译失败。缓存达到 `CACHE_MAX_ENTRIES` 上限时会淘汰最接近过期的条目，写入失败也只降级为「仅使用数据库缓存」，此前会把异常抛给调用方，导致译文已生成却报错并多消耗一次模型调用。
- `CACHE_TTL` 小于 10 秒时不再关闭过期清扫，避免缓存永久处于写满状态。
- 修复未使用文件清理接口。此前遇到 `epub-resources/` 等子目录会直接中断，一个孤儿文件都清理不掉；存储用量统计也不再把子目录计入。
- 全书摘要不再重复插入记录。此前每次重新生成都会新增一行，界面上会看到重复条目；升级时会自动合并已有重复数据，优先保留已完成的记录。
- 上传去重不再误判。同名同大小但内容不同的文件会正常入库，此前会被当作重复上传丢弃，书架上仍显示旧版本。

### 移除

- Dependabot 依赖更新配置。依赖升级改为手动进行；生产依赖漏洞仍由 `npm run check` 中的 `npm audit --omit=dev --audit-level=high` 兜底。

## [0.0.1] - 2026-08-26

### 新增

- EPUB/PDF 本地书库、阅读进度、文件夹和 PDF 原版阅读。
- 单页/批量翻译、摘要、阅读导读、思维导图和书籍问答。
- OpenAI 兼容、Anthropic 兼容和 Ollama 原生模型服务。
- FolioPaw、Ollama 和模型引导服务的 Docker Compose 一键部署。
- Ollama 官方源与固定 ModelScope GGUF 回退，支持断点续传、SHA-256 校验和离线导入。
- schema v3 数据库迁移、迁移前备份和 Docker 托管模型配置。
- GitHub Actions、Dependabot、Issue/PR 模板、安全政策和第三方许可声明。

### 变更

- 用户界面、公开 API 提示、模型引导状态、AI 输出约束和开源文档统一使用简体中文。
- AI 长文本使用统一上下文预算；摘要、翻译、导读、思维导图和问答会按任务自动拆分或裁剪。
- 项目名称、应用界面、包元数据和容器服务统一使用 FolioPaw 品牌。
- Compose 内部项目 ID 和数据卷键保留旧标识，确保已有本地数据无迁移升级。
- Docker 仅向宿主机发布 FolioPaw 的 localhost 端口，Ollama 和引导接口保留在容器网络内。
- 默认开发/Docker 入口改为 `127.0.0.1:17890`，避免与 macOS AirPlay 的 5000 端口冲突。
- 第三方模型 API 只在设置页配置，不写入 `.env`。

### 移除

- 注册、登录和多用户能力；FolioPaw 明确为单设备、单人本地书库。
- Codex CLI 模型调用和本地进程配置。
