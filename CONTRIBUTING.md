# 贡献指南

感谢你帮助改进 FolioPaw。请先搜索现有 Issue。

## 开发流程

1. 使用 Node.js 22.12 或更新版本。
2. Fork/克隆仓库并执行 `npm ci`。
3. 执行 `npm run dev`；第三方模型和 API Key 仅在应用的“设置 → 模型服务”中配置。
4. 在主题分支完成一项聚焦的改动。
5. 提交前运行 `npm run check`。
6. PR 中说明目的、行为变化、测试方式；UI 变化附前后截图。

## 数据与测试要求

- 只能使用测试运行时生成或明确允许再分发的 EPUB；涉及遗留 PDF 阅读代码时也必须使用合成 PDF。
- 不得提交数据库、模型 Key、上传书籍、译文、日志或个人绝对路径。
- 安全修复应增加回归测试。上传相关测试至少考虑伪造格式、路径穿越、压缩炸弹、体积限制和失败清理。
- API 变更需同步 README；配置变更需同步 `.env.example`。
- 模型引导测试必须使用模拟 Ollama 和微型文件，CI 不得下载真实 3 GB 权重；Docker 变更需校验 CPU、NVIDIA、AMD Compose 合并结果。

## 代码约定

- 保持无账号、本机优先的安全边界；不要在应用内重新引入账号体系。扩大网络暴露前需要先讨论威胁模型。
- 避免 `any`、未校验的外部输入、公开静态上传目录和正则式 HTML/SVG 清理。
- 数据访问必须保持单一本地书库边界，并兼容既有数据库升级。
- 新依赖应说明必要性、许可证，并保证生产依赖无 high/critical 漏洞。

安全问题请按 [`SECURITY.md`](SECURITY.md) 私密报告，不要在公开 Issue 中提交漏洞细节。

参与项目即表示你同意遵守 [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)。
